# Test fixtures

- **`minimal_dataset/`** — Two tiny RGB classes (`fixture_a`, `fixture_b`) for `ML/test_train_validation.py` when `data/` is empty or when `SLI_VALIDATION_DATA_DIR` is unset.
- **`minimal_mapping.yaml`** — Gloss map for dry-run tests of `ML/ingest_external.py`.
