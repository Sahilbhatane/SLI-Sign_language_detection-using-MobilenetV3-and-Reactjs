import React from 'react';
import DetectionDisplay from './DetectionDisplay';

const DetectionPanel = ({ detection }) => {
  return <DetectionDisplay detection={detection} />;
};

export default DetectionPanel;
